// js Output => innerHTML,document.write,window.alert,window.print,console.log

   

    // ******************** variable *******************************
    // let and const have block scope.
    // let and const can not be redeclared.
    // let and const must be declared before use.
    // let and const does not bind to this.
    // let and const are not hoisted.
    // var does not have to be declared.
    // var is hoisted.
    // var binds to this.

    // ********************* Data type *********************************
    // permitive (stack meamory refrencece) string,number,bigint,boolean,undefined,null,symbol
    // non-permitive (Heap meamory refrence) Array,function,object

    //******************** Operator ****************************
    // Arthematic operator : +,-,*,/,**,%,++,--
    // Assignement operator: =,+=,-+,*=,/=,%=,**=
    // comparison Operator : ==,===,!=,!==,>,<,<=,>=,?(ternary Operator)
    // Ternary operator    : condition ? true:false
    // Logical Operator    : &&(multiple true=true) || (just one true=true) !(opposite)
   
    // *************************Data Type*********************************
    // let a = null;   // explicitly set to null no memory address
    // let b;          // declared but not assigned, so it's undefined no memory address
    